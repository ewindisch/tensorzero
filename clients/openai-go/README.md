# `openai-go` Client Compatibility Tests

This directory contains tests for OpenAI compatibility using the official OpenAI Go client.

## Setup

1. Install the dependencies: `go get`
2. Make sure TensorZero is running locally on port 3000

## Testing

```bash
go test -v
```
